/*export interface Theme {
    name: string;
    properties: any;
}

export const light: Theme = {
    name: "light",
    properties: {
        "--theme-bg-color": "#{$theme-light-bg-color}",
        "--theme-background": "#{$theme-light-background}",
        "--theme-blue": "#{$theme-light-blue}",
        "--theme-grey": "#{$theme-light-grey}"
    }
};

export const dark: Theme = {
    name: "dark",
    properties: {
        "--theme-bg-color": "#1E202A",
        "--theme-background": "#252A41",
        "--theme-blue": "#1E202A",
        "--theme-grey": "#8B97C6"
    }
}; */